<%= form_with(model: circle_graph, local: true) do |form| %>
  <% if circle_graph.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(circle_graph.errors.count, "error") %> prohibited this circle_graph from being saved:</h2>

      <ul>
      <% circle_graph.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :score %>
    <%= form.number_field :score, placeholder: "Enter a score" %>
  </div>

  <div class="field">
    <%= form.label :total %>
    <%= form.number_field :total, placeholder: "Enter a total" %>
  </div>

  <div class="actions">
    <%= form.submit class: "button purple" %>
  </div>
<% end %>
