<%= form_with(model: line_graph, local: true) do |form| %>
  <% if line_graph.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(line_graph.errors.count, "error") %> prohibited this line_graph from being saved:</h2>

      <ul>
      <% line_graph.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :scores %>
    <%= form.text_field :scores,
          placeholder: "x, y, z",
          autofocus: true,
          aria: { "described-by": "scores-error" } %>
    <p class="error" id="scores-error">
      <%= line_graph.errors[:scores].first %>
    </p>
  </div>

  <div class="field">
    <%= form.label :maximum %>
    <%= form.number_field :maximum,
          placeholder: "Enter a score",
          aria: { "described-by": "maximum-error" } %>
    <p class="error" id="maximum-error">
      <%= line_graph.errors[:maximum].first %>
    </p>
  </div>

  <div class="actions">
    <%= form.submit class: "button purple" %>
  </div>
<% end %>
